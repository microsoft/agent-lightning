env_name: babyai # scienceworld, babyai, alfworld
seed: 0
format_penalty: 0.0
binary_reward: False
captioner:
  type: naive # naive or cot
  obs_type: chat # chat or single
  max_text_history: 16
  
babyai_kwargs:
  num_dists: 0
  # max_steps: 64