# Gateway config for the ScienceWorld example.
#
# Injects ``return_token_ids: true`` so vLLM returns prompt_token_ids and
# token_ids on every response — VERL needs them for PPO/GRPO updates.

routes:
  - model_in: "*"
    model_out: "*"
    params:
      add:
        return_token_ids: true
